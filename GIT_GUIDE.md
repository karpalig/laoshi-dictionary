# 📘 Git для начинающих - Пошаговое руководство

## 🎯 Ваша цель
Работать в одной понятной ветке и публиковать на GitHub Pages.

---

## ✅ РЕКОМЕНДУЕМЫЙ СПОСОБ - Работать в main

### Шаг 1: Переключить GitHub Pages на main

**Вручную (через браузер):**

1. Откройте: https://github.com/karpalig/laoshi-dictionary/settings/pages
2. В разделе **"Source"** (Источник):
   - **Branch:** выберите `main` (вместо cursor/chinese...)
   - **Folder:** выберите `/ (root)`
3. Нажмите **Save**
4. Подождите 5 минут

**Готово!** Теперь GitHub Pages публикуется из ветки `main`.

---

### Шаг 2: Ваш рабочий процесс

```bash
# 1. ВСЕГДА проверяйте, что вы в main
git branch
# Должно показать: * main

# 2. Перед работой - получите последние изменения
git pull origin main

# 3. Работайте с файлами (редактируйте код)
# ...

# 4. Посмотрите что изменилось
git status

# 5. Добавьте изменения
git add .

# 6. Сохраните изменения с описанием
git commit -m "Описание что вы сделали"

# 7. Отправьте на GitHub
git push origin main

# 8. Подождите 5-10 минут - GitHub Pages обновится
```

---

## 🌿 Ветки - Простое объяснение

### Что такое ветка?

Представьте книгу с несколькими версиями:

```
📚 Книга (Репозиторий)
├── 📖 Основная версия (main)
├── 📖 Черновик 1 (feature-new-design)
└── 📖 Черновик 2 (bugfix)
```

**Ветка** = отдельная версия вашего кода.

### Зачем нужны ветки?

**Основная ветка (main/master):**
- ✅ Рабочий, проверенный код
- ✅ Публикуется на сайте
- ✅ Пользователи видят эту версию

**Дополнительные ветки:**
- 🧪 Эксперименты
- 🔧 Разработка новых функций
- 🐛 Исправление ошибок

---

## 🎓 Основные команды Git

### Проверить текущую ветку
```bash
git branch
# Показывает список веток
# * main  ← звездочка = вы здесь
```

### Переключиться на другую ветку
```bash
git checkout main
# Переключиться на main

git checkout cursor/chinese-russian-dictionary-app-c274
# Переключиться на cursor ветку
```

### Создать новую ветку
```bash
git checkout -b new-feature
# Создать и переключиться на новую ветку
```

### Посмотреть статус
```bash
git status
# Показывает:
# - Какие файлы изменены
# - Какие готовы к коммиту
# - На какой ветке вы находитесь
```

### Посмотреть историю
```bash
git log --oneline
# Показывает последние коммиты
```

---

## 📋 Ваш ежедневный чеклист

**Перед началом работы:**
```bash
# 1. Проверить ветку
git branch

# 2. Если не в main - переключиться
git checkout main

# 3. Получить последние изменения
git pull origin main
```

**После работы:**
```bash
# 1. Проверить что изменилось
git status

# 2. Добавить все изменения
git add .

# 3. Сохранить с комментарием
git commit -m "Добавил новый словарь HSK 2"

# 4. Отправить на GitHub
git push origin main
```

---

## 🚨 Частые ошибки и решения

### Ошибка: "Your branch is behind"
```bash
# Решение: получите последние изменения
git pull origin main
```

### Ошибка: "Uncommitted changes"
```bash
# Решение 1: Сохранить изменения
git add .
git commit -m "WIP: работа в процессе"

# Решение 2: Отменить изменения (ОСТОРОЖНО!)
git checkout -- .
```

### Ошибка: "Conflict" (конфликт)
```bash
# Когда: Вы и кто-то другой изменили один файл

# Решение:
# 1. Откройте файл с конфликтом
# 2. Найдите маркеры:
#    <<<<<<< HEAD
#    ваш код
#    =======
#    чужой код
#    >>>>>>>
# 3. Выберите правильный вариант
# 4. Удалите маркеры
# 5. Сохраните:
git add .
git commit -m "Resolve conflict"
```

---

## 🎯 Специально для вашего проекта

### Сейчас у вас две ветки:

**1. main** ← Работайте здесь!
- Ваша основная рабочая ветка
- Все новые изменения здесь
- Понятное название

**2. cursor/chinese-russian-dictionary-app-c274** 
- Автоматически созданная Cursor
- Настроена для GitHub Pages
- Можно удалить после настройки

---

## 🔧 План действий

### Шаг 1: Настроить Pages на main

**Через браузер:**
1. https://github.com/karpalig/laoshi-dictionary/settings/pages
2. Source: **main** → **/ (root)** → Save

### Шаг 2: Удалить cursor ветку (опционально)

```bash
# Локально
git branch -d cursor/chinese-russian-dictionary-app-c274

# На GitHub (осторожно!)
git push origin --delete cursor/chinese-russian-dictionary-app-c274
```

⚠️ **НЕ УДАЛЯЙТЕ**, пока не убедитесь что Pages работает с main!

### Шаг 3: Всегда работайте в main

```bash
# Проверка перед работой
git branch
# Должно быть: * main

# Если нет:
git checkout main
```

---

## 📚 Полезные ресурсы

### Визуализация Git:
- https://git-school.github.io/visualizing-git/ - интерактивная визуализация

### Шпаргалки:
- https://education.github.com/git-cheat-sheet-education.pdf

### Интерактивный туториал:
- https://learngitbranching.js.org/?locale=ru_RU (на русском!)

---

## 💡 Золотые правила Git

1. **ВСЕГДА проверяйте ветку перед работой** (`git branch`)
2. **Коммитьте часто** - маленькие изменения легче отследить
3. **Пишите понятные сообщения коммитов**
   - ❌ "fix"
   - ✅ "Исправлена ошибка загрузки HSK 1"
4. **Делайте pull перед push** (`git pull` → работа → `git push`)
5. **Не бойтесь ошибок** - всё можно откатить!

---

## 🆘 Если что-то сломалось

### Откат последнего коммита (не запушенного)
```bash
git reset HEAD~1
```

### Откат к определенному коммиту
```bash
# Посмотреть историю
git log --oneline

# Откатиться к коммиту
git reset --hard abc123
```

### Отменить все локальные изменения
```bash
git reset --hard origin/main
```

⚠️ **ОСТОРОЖНО:** `--hard` удаляет все незакоммиченные изменения!

---

## 🎓 Следующий уровень

Когда освоите основы, изучите:
- **Merge** - объединение веток
- **Rebase** - перебазирование
- **Stash** - временное сохранение
- **Cherry-pick** - выборочный перенос коммитов
- **.gitignore** - игнорирование файлов

Но пока **просто работайте в main** - этого достаточно! ✅

---

## 📞 Быстрая справка

```bash
# Основные команды
git status          # Что происходит?
git branch          # Где я?
git pull            # Получить изменения
git add .           # Добавить все
git commit -m "..."  # Сохранить
git push            # Отправить на GitHub

# Переключение веток
git checkout main   # На main
git checkout -b new # Создать новую

# История
git log --oneline   # Последние коммиты
git diff            # Что изменилось?

# Помощь
git help            # Список команд
git help commit     # Справка по команде
```

---

**Удачи в освоении Git!** 🚀

**记住：实践出真知** (Практика - путь к знанию!) 🇨🇳
