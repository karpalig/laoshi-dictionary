# 💻 Настройка Cursor для работы с проектом (Windows)

## 🎯 Быстрый старт

### Шаг 1: Откройте проект в Cursor

1. **Запустите Cursor**
2. **File → Open Folder** (или `Ctrl+K Ctrl+O`)
3. **Выберите папку** где склонирован проект:
   ```
   C:\Users\ВашеИмя\laoshi-dictionary
   ```

---

## 🌿 Работа с ветками в Cursor

### Способ 1: Через интерфейс (самый простой)

1. **Смотрите в левый нижний угол** Cursor
2. Там написано название текущей ветки, например:
   ```
   🌿 main
   ```
   или
   ```
   🌿 cursor/chinese-russian-dictionary-app-c274
   ```

3. **Кликните на название ветки**
4. Появится меню:
   ```
   ✓ main
     cursor/chinese-russian-dictionary-app-c274
     + Create new branch...
   ```

5. **Выберите `main`** - это ваша рабочая ветка

---

### Способ 2: Через командную палитру

1. Нажмите **`Ctrl+Shift+P`**
2. Введите: `Git: Checkout to`
3. Выберите **`main`**

---

### Способ 3: Через терминал в Cursor

1. Нажмите **`Ctrl+``** (обратная кавычка) - откроется терминал
2. Введите:
   ```bash
   git checkout main
   ```

---

## 🔄 Базовый рабочий процесс

### Перед началом работы (каждый раз!)

1. **Убедитесь что вы в main:**
   - Смотрите левый нижний угол: должно быть `🌿 main`

2. **Получите последние изменения:**
   - Нажмите **`Ctrl+Shift+P`**
   - Введите: `Git: Pull`
   - Или в терминале: `git pull origin main`

---

### Во время работы

#### Просмотр изменений:

1. **Боковая панель "Source Control"**:
   - Иконка с веткой (3-я сверху)
   - Или `Ctrl+Shift+G`

2. **Там видны все измененные файлы:**
   ```
   📝 Changes (3)
   ├── 📄 index.html (M)
   ├── 📄 js/app.js (M)
   └── 📄 README.md (M)
   ```

#### Сохранение изменений (коммит):

1. **В Source Control панели:**
   - Наведите на файл → нажмите **`+`** (Stage Changes)
   - Или нажмите **`+`** рядом с "Changes" чтобы добавить все

2. **Введите сообщение** в поле вверху:
   ```
   Добавил кнопку загрузки HSK 2
   ```

3. **Нажмите `✓ Commit`** или `Ctrl+Enter`

#### Отправка на GitHub (push):

1. **После коммита** нажмите кнопку **"Sync Changes"** (⟳)
2. Или через меню:
   - `Ctrl+Shift+P`
   - `Git: Push`

---

## 🎨 Интерфейс Cursor - Где что находится

```
┌─────────────────────────────────────────────────┐
│  Cursor                            [- □ ×]      │ ← Заголовок
├──┬──────────────────────────────────────────────┤
│  │  📁 EXPLORER                                  │
│ 📁│  └── laoshi-dictionary/                     │ ← Файлы проекта
│  │      ├── css/                                │
│ 🔍│      ├── js/                                │
│  │      ├── examples/                           │
│ 🌿│      └── index.html                         │
│  │                                               │
│ ⚙️│  📝 index.html                              │ ← Редактор
│  │  ┌─────────────────────────────┐            │
│  │  │ HTML код здесь...           │            │
│  │  │                              │            │
│  │  └─────────────────────────────┘            │
├──┴──────────────────────────────────────────────┤
│  > Terminal (bash)                              │ ← Терминал
│  $ git status                                   │
└─┬───────────────────────────────────────────────┘
  │ 🌿 main ↓0 ↑0  ⚠️ 0  Ln 42, Col 12           │ ← Статус бар
  └───────────────────────────────────────────────┘
```

### Боковая панель (слева):
- 📁 **Explorer** - файлы проекта
- 🔍 **Search** - поиск по файлам
- 🌿 **Source Control** - Git (изменения, коммиты)
- 🐛 **Debug** - отладка (не нужна для этого проекта)
- ⚙️ **Extensions** - расширения

### Статус бар (внизу):
- 🌿 **main** - текущая ветка (КЛИКАБЕЛЬНО!)
- ↓0 ↑0 - входящие/исходящие коммиты
- ⚠️ 0 - ошибки/предупреждения

---

## ⚡ Горячие клавиши в Cursor

### Общие:
```
Ctrl+P          - Быстрый поиск файла
Ctrl+Shift+P    - Командная палитра
Ctrl+`          - Открыть/закрыть терминал
Ctrl+B          - Показать/скрыть боковую панель
Ctrl+\          - Разделить редактор
```

### Git:
```
Ctrl+Shift+G    - Открыть Source Control
Ctrl+Enter      - Коммит (в поле сообщения)
Ctrl+Shift+P → Git: Pull   - Получить изменения
Ctrl+Shift+P → Git: Push   - Отправить изменения
```

### Редактирование:
```
Ctrl+S          - Сохранить файл
Ctrl+Z          - Отменить
Ctrl+Shift+Z    - Вернуть
Ctrl+F          - Найти в файле
Ctrl+H          - Найти и заменить
```

---

## 🚀 Запуск локального сервера из Cursor

### Вариант 1: Через терминал Cursor

1. **Откройте терминал**: `Ctrl+``
2. **Запустите сервер:**
   ```bash
   python -m http.server 8000
   ```
3. **Откройте браузер:**
   ```
   http://localhost:8000/direct.html
   ```

### Вариант 2: Через задачу (Task)

Создайте файл `.vscode/tasks.json`:

```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start Local Server",
      "type": "shell",
      "command": "python -m http.server 8000",
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "new"
      }
    }
  ]
}
```

Затем: `Ctrl+Shift+P` → `Tasks: Run Task` → `Start Local Server`

### Вариант 3: Live Server расширение

1. **Установите расширение:**
   - `Ctrl+Shift+X` (Extensions)
   - Найдите: `Live Server`
   - Нажмите **Install**

2. **Запуск:**
   - Откройте `index.html`
   - Правый клик → **Open with Live Server**
   - Автоматически откроется браузер с автообновлением!

---

## 📁 Рекомендуемая структура работы

### Создайте рабочее пространство:

1. **File → Add Folder to Workspace**
2. **Выберите папку** `laoshi-dictionary`
3. **File → Save Workspace As...**
4. Сохраните как `chinese-dictionary.code-workspace`

### Преимущества:
- ✅ Быстрый доступ к проекту
- ✅ Сохраняются настройки
- ✅ Открывается двойным кликом

---

## 🔧 Настройка Git в Cursor

### Проверка конфигурации Git

Откройте терминал (`Ctrl+``) и выполните:

```bash
# Проверить имя
git config user.name

# Проверить email
git config user.email

# Если не настроено - установите:
git config --global user.name "Ваше Имя"
git config --global user.email "your@email.com"
```

### Аутентификация GitHub

Если Cursor просит логин/пароль при push:

**Вариант 1: Personal Access Token (рекомендуется)**

1. Откройте: https://github.com/settings/tokens
2. **Generate new token** → **Classic**
3. Выберите права: `repo` (все подпункты)
4. **Generate token** → скопируйте токен
5. При push используйте токен вместо пароля

**Вариант 2: GitHub CLI (проще)**

```bash
# Установите GitHub CLI
# https://cli.github.com/

# Авторизуйтесь
gh auth login
```

**Вариант 3: SSH (продвинутый)**

```bash
# Генерация SSH ключа
ssh-keygen -t ed25519 -C "your@email.com"

# Добавление ключа на GitHub
# https://github.com/settings/keys
```

---

## 📋 Ежедневный чеклист

### ☀️ Утро (начало работы):

```bash
# 1. Откройте Cursor
# 2. Откройте проект (File → Open Recent)
# 3. В терминале:
git checkout main
git pull origin main

# 4. Готово к работе!
```

### 🌙 Вечер (конец работы):

```bash
# 1. В Source Control (Ctrl+Shift+G):
#    - Stage changes (+)
#    - Введите сообщение коммита
#    - Commit (✓)
#    - Sync Changes (⟳)

# 2. Или в терминале:
git add .
git commit -m "Описание работы за день"
git push origin main

# 3. Проверьте на GitHub:
#    https://github.com/karpalig/laoshi-dictionary
```

---

## 🐛 Частые проблемы в Cursor

### Cursor не видит Git

**Решение:**
1. Проверьте что Git установлен: `git --version`
2. Если нет - установите: https://git-scm.com/download/win
3. Перезапустите Cursor

### "Permission denied" при push

**Решение:**
- Настройте аутентификацию (см. выше)
- Используйте Personal Access Token

### Конфликты при pull

**Решение в Cursor:**
1. Source Control покажет конфликты
2. Откройте файл с конфликтом
3. Cursor покажет кнопки:
   - **Accept Current** (ваша версия)
   - **Accept Incoming** (версия с GitHub)
   - **Accept Both** (обе версии)
4. Выберите нужное
5. Сохраните → Commit

---

## 💡 Полезные расширения для Cursor

### Обязательные:

1. **Live Server** - локальный сервер с автообновлением
2. **GitLens** - расширенные возможности Git
3. **Prettier** - автоформатирование кода

### Опциональные:

4. **Git Graph** - визуализация веток
5. **TODO Highlight** - подсветка TODO комментариев
6. **Path Intellisense** - автодополнение путей

### Установка:
`Ctrl+Shift+X` → найти → Install

---

## 🎯 Workflow для вашего проекта

### Типичная рабочая сессия:

```
1. Открыть Cursor
   ↓
2. Проверить ветку (main)
   ↓
3. git pull (получить изменения)
   ↓
4. Редактировать файлы
   ↓
5. Тестировать локально (python -m http.server)
   ↓
6. Source Control → Stage → Commit
   ↓
7. Sync Changes (push)
   ↓
8. Подождать 5-10 мин → проверить на GitHub Pages
```

---

## 📚 Настройка для вашего проекта

### Создайте файл `.vscode/settings.json`:

```json
{
  "editor.formatOnSave": true,
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000,
  "git.enableSmartCommit": true,
  "git.confirmSync": false,
  "terminal.integrated.defaultProfile.windows": "Git Bash",
  "liveServer.settings.port": 8000
}
```

### Создайте файл `.vscode/extensions.json`:

```json
{
  "recommendations": [
    "ritwickdey.liveserver",
    "eamodio.gitlens",
    "esbenp.prettier-vscode"
  ]
}
```

Cursor автоматически предложит установить эти расширения.

---

## 🆘 Если что-то пошло не так

### Откат изменений:

**В интерфейсе Cursor:**
1. Source Control → правый клик на файле
2. **Discard Changes** - отменить изменения файла

**В терминале:**
```bash
# Отменить изменения одного файла
git checkout -- filename.js

# Отменить все изменения
git checkout -- .
```

### Отменить последний коммит:

```bash
# Отменить но оставить изменения
git reset HEAD~1

# Отменить и удалить изменения (осторожно!)
git reset --hard HEAD~1
```

---

## 📞 Быстрая справка

### Cursor интерфейс:
```
Ctrl+P          - Поиск файла
Ctrl+Shift+P    - Команды
Ctrl+`          - Терминал
Ctrl+Shift+G    - Git
```

### Git команды:
```
git status      - Что изменилось?
git pull        - Получить изменения
git add .       - Добавить все
git commit -m   - Сохранить
git push        - Отправить
```

### Ветки:
```
git branch      - Список веток
git checkout    - Переключиться
```

---

## 🎓 Дальнейшее обучение

**Официальная документация:**
- Cursor: https://cursor.sh/docs
- Git: https://git-scm.com/doc

**Интерактивные туториалы:**
- Git: https://learngitbranching.js.org/?locale=ru_RU
- VS Code (похож на Cursor): https://code.visualstudio.com/docs

---

## ✅ Проверка что всё настроено

Выполните в терминале Cursor:

```bash
# 1. Git установлен?
git --version
# Должно показать: git version 2.x.x

# 2. Настроено имя?
git config user.name
# Должно показать ваше имя

# 3. В правильной ветке?
git branch
# Должно показать: * main

# 4. Синхронизировано?
git status
# Должно показать: nothing to commit, working tree clean
```

Если всё ✅ - **готовы к работе!**

---

**Удачи в разработке! 加油！** 🚀
