# 🪟 Быстрый старт для Windows

## Установка Flutter на Windows

### Шаг 1: Скачать Flutter

1. Перейдите на https://flutter.dev/docs/get-started/install/windows
2. Скачайте Flutter SDK (zip файл)
3. Распакуйте в `C:\src\flutter` (или любую папку без пробелов)

### Шаг 2: Добавить Flutter в PATH

1. Откройте **Панель управления** → **Система** → **Дополнительные параметры системы**
2. Нажмите **Переменные среды**
3. В разделе **Системные переменные** найдите `Path` и нажмите **Изменить**
4. Добавьте путь к Flutter: `C:\src\flutter\bin`
5. Нажмите **OK** во всех окнах

### Шаг 3: Проверить установку

Откройте Command Prompt (cmd) и выполните:

```cmd
flutter doctor
```

Вы увидите проверку всех компонентов:
- ✓ Flutter
- ✓ Windows toolchain
- ✗ Android toolchain (опционально)
- ✗ Visual Studio (требуется для Windows apps)

### Шаг 4: Установить Visual Studio (для Windows apps)

1. Скачайте **Visual Studio 2022 Community** (бесплатно)
2. При установке выберите:
   - ✅ Desktop development with C++
   - ✅ Windows 10/11 SDK
3. Установите

Проверьте снова:
```cmd
flutter doctor
```

Теперь Windows toolchain должен быть ✓

## Запуск приложения

### 1. Откройте папку проекта

```cmd
cd \workspace\flutter_dictionary
```

### 2. Установите зависимости

```cmd
flutter pub get
```

### 3. Запустите приложение

```cmd
flutter run -d windows
```

Приложение откроется в окне Windows!

## Сборка для Windows

### Debug версия (для тестирования)

```cmd
flutter build windows
```

Результат: `build\windows\runner\Release\`

### Release версия (для распространения)

```cmd
flutter build windows --release
```

Содержимое папки `build\windows\runner\Release\` готово к распространению:
- `chinese_russian_dictionary.exe` - основной исполняемый файл
- `data\` - ресурсы приложения
- `flutter_windows.dll` - Flutter runtime
- Другие DLL файлы

## Создание инсталлятора (опционально)

### Используя Inno Setup

1. Скачайте Inno Setup: https://jrsoftware.org/isdl.php
2. Создайте скрипт `installer.iss`:

```iss
[Setup]
AppName=汉俄词典
AppVersion=1.0
DefaultDirName={pf}\ChineseRussianDictionary
DefaultGroupName=Chinese Dictionary
OutputDir=installer
OutputBaseFilename=ChineseDictionarySetup

[Files]
Source: "build\windows\runner\Release\*"; DestDir: "{app}"; Flags: recursesubdirs

[Icons]
Name: "{group}\汉俄词典"; Filename: "{app}\chinese_russian_dictionary.exe"
Name: "{commondesktop}\汉俄词典"; Filename: "{app}\chinese_russian_dictionary.exe"
```

3. Запустите Inno Setup и скомпилируйте скрипт

## Устранение проблем

### Ошибка: "Flutter command not found"
→ Flutter не добавлен в PATH. Повторите Шаг 2.

### Ошибка: "Visual Studio not found"
→ Установите Visual Studio 2022 с C++ workload.

### Ошибка: "Unable to find sqlite3.dll"
→ Выполните:
```cmd
flutter clean
flutter pub get
flutter run -d windows
```

### Приложение медленно запускается
→ Это нормально в debug режиме. Используйте release сборку:
```cmd
flutter run -d windows --release
```

### База данных не сохраняется
→ Проверьте путь:
```
C:\Users\[ВашеИмя]\Documents\ChineseRussianDictionary\dictionary.db
```

## Горячие клавиши (в debug режиме)

- `r` - Hot reload (быстрая перезагрузка)
- `R` - Hot restart (полная перезагрузка)
- `q` - Выход

## Системные требования

**Минимальные:**
- Windows 7 SP1 или новее
- 2 GB RAM
- 1 GB свободного места

**Рекомендуемые:**
- Windows 10/11
- 4 GB RAM
- SSD диск

## Производительность

- Запуск: ~1-2 секунды
- Поиск: мгновенный
- Размер приложения: ~20-30 MB
- Потребление RAM: ~50-100 MB

## Распространение

Для распространения приложения скопируйте всю папку:
```
build\windows\runner\Release\
```

Пользователи смогут запустить `chinese_russian_dictionary.exe`.

**Внимание:** Некоторым пользователям может потребоваться **Visual C++ Redistributable**:
https://aka.ms/vs/17/release/vc_redist.x64.exe

## Обновление Flutter

```cmd
flutter upgrade
```

После обновления:
```cmd
cd \workspace\flutter_dictionary
flutter clean
flutter pub get
flutter run -d windows
```

## Полезные команды

```cmd
# Проверка установки
flutter doctor -v

# Очистка проекта
flutter clean

# Обновление зависимостей
flutter pub get

# Анализ кода
flutter analyze

# Запуск тестов
flutter test

# Проверка производительности
flutter run --profile

# Список устройств
flutter devices
```

## 🎉 Готово!

Теперь у вас полностью рабочее приложение на Windows!

Основные действия:
1. **Поиск** - введите китайский, пиньинь или русский
2. **Словари** - создайте и управляйте словарями
3. **Избранное** - сохраняйте важные слова

**Удачи в изучении китайского! 加油！** 🇨🇳
