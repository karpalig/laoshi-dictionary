# 🚀 Локальная разработка и тестирование

## Проблема
GitHub Pages обновляется медленно (5-30 минут), Service Worker кеширует старые версии.

## ✅ Решение: Локальный сервер

---

## 📦 Вариант 1: Python (рекомендуется)

### Запуск:
```bash
cd /workspace
python3 -m http.server 8000
```

### Открыть:
👉 **http://localhost:8000/index.html**

### Остановить:
`Ctrl+C`

---

## 📦 Вариант 2: Node.js (npx)

### Запуск:
```bash
cd /workspace
npx serve
```

### Открыть:
Откройте URL который покажет в консоли (обычно http://localhost:3000)

---

## 📦 Вариант 3: PHP

### Запуск:
```bash
cd /workspace
php -S localhost:8000
```

### Открыть:
👉 **http://localhost:8000/index.html**

---

## 🔧 Для вас (пошагово)

### Шаг 1: Откройте терминал в Windows

**PowerShell:**
```powershell
# Перейдите в папку проекта
cd C:\Users\ВашИмя\laoshi-dictionary

# Запустите сервер
python -m http.server 8000
```

**Или Git Bash:**
```bash
cd /c/Users/ВашИмя/laoshi-dictionary
python -m http.server 8000
```

### Шаг 2: Откройте в браузере

👉 **http://localhost:8000/index.html**

### Шаг 3: Тестируйте

1. Перейдите на вкладку **"Словари"**
2. Вы должны увидеть кнопки:
   - 📗 HSK 1 (150 слов)
   - 🎯 Примеры (5 слов)
3. Нажмите любую кнопку - должна загрузиться

---

## 🔄 Автоперезагрузка при изменениях

### Live Server (VS Code расширение)

1. Установите расширение **Live Server** в VS Code
2. Откройте `index.html`
3. Правый клик → **Open with Live Server**
4. Автоматическая перезагрузка при сохранении файлов!

### Browser-sync (Node.js)

```bash
npm install -g browser-sync
cd /workspace
browser-sync start --server --files "*.html, css/*.css, js/*.js"
```

Автоматически откроется браузер с автообновлением!

---

## 📁 Структура для разработки

```
/workspace/
├── index.html          # Основная версия (с Service Worker)
├── direct.html         # Прямая версия (без SW)
├── css/
│   └── styles.css
├── js/
│   ├── app.js
│   ├── db.js
│   └── pinyin.js
├── examples/
│   ├── hsk1_basic.json
│   ├── hsk1_from_clem.json
│   ├── hsk2_from_clem.json
│   └── ... (все словари)
└── sw.js               # Service Worker
```

---

## 🐛 Отладка

### Консоль браузера (F12)

**Проверьте:**
```javascript
// Список словарей
console.log(app.dictionaries);

// Список слов
console.log(app.words);

// Тест загрузки файла
fetch('examples/hsk1_basic.json')
  .then(r => r.json())
  .then(data => console.log('✅ Загружено:', data));
```

### Проверка Service Worker

```javascript
// Удалить Service Worker
navigator.serviceWorker.getRegistrations()
  .then(r => r.forEach(reg => reg.unregister()));

// Очистить кеш
caches.keys().then(k => k.forEach(name => caches.delete(name)));
```

---

## 🎨 Разработка без кеша

### Используйте direct.html

Для разработки используйте версию БЕЗ Service Worker:

```bash
# Откройте
http://localhost:8000/direct.html
```

Эта версия **НЕ кеширует** файлы - всегда актуальная!

---

## 🔗 Синхронизация с GitHub

### После локального тестирования:

```bash
# 1. Коммит изменений
git add .
git commit -m "Your changes"

# 2. Пуш на GitHub
git push origin main

# 3. Подождите 5-10 минут для GitHub Pages
```

### Проверка обновления:

1. Откройте: https://github.com/karpalig/laoshi-dictionary/actions
2. Дождитесь зеленой галочки ✅
3. Проверьте сайт с жесткой перезагрузкой (Ctrl+Shift+R)

---

## 💡 Рекомендации

### Для разработки:
- ✅ Используйте **локальный сервер** (Python/Node)
- ✅ Открывайте **direct.html** (без Service Worker)
- ✅ Держите консоль открытой (F12) для отладки

### Для продакшена:
- ✅ Тестируйте **index.html** с Service Worker
- ✅ Проверяйте на разных браузерах
- ✅ Очищайте кеш перед финальным тестом

### Для пользователей:
- ✅ Публикуйте на GitHub Pages
- ✅ Предоставляйте ссылку на **direct.html** для первого визита
- ✅ После обновления увеличивайте версию кеша в `sw.js`

---

## 📊 Checklist перед деплоем

- [ ] Протестировано локально
- [ ] Работает в режиме инкогнито
- [ ] Консоль без ошибок
- [ ] Все кнопки видны и работают
- [ ] Словари загружаются
- [ ] Поиск работает
- [ ] Импорт/Экспорт работает
- [ ] Service Worker версия обновлена (sw.js)
- [ ] Закоммичено и запушено
- [ ] GitHub Actions прошел успешно ✅

---

## 🆘 Частые проблемы

### Кнопки не видны локально
→ Проверьте что открываете правильный файл (index.html или direct.html)

### Файлы не загружаются (404)
→ Проверьте путь: должны быть в `examples/`
→ Запускайте сервер из корня проекта (/workspace)

### Изменения не применяются
→ Очистите кеш браузера (Ctrl+Shift+R)
→ Используйте режим инкогнито
→ Используйте direct.html для разработки

### CORS ошибки
→ Не открывайте файлы напрямую (file:///)
→ Используйте локальный сервер (http://localhost)

---

## 📞 Нужна помощь?

1. Откройте консоль (F12)
2. Скопируйте все ошибки
3. Создайте Issue на GitHub
4. Приложите скриншоты

---

**Удачи в разработке!** 🚀
